name: Test Reusable3

on:
  workflow_dispatch:
  push:
    branches:
      - trae/agent-arUNOy
    paths:
      - ".github/trigger-reusable3.txt"

jobs:
  test:
    uses: ./.github/workflows/flutter-build-test3.yml
    with:
      upload-artifact: false
      upload-tag: "test"
