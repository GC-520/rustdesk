name: Flutter Build Test3

on:
  workflow_call:
    inputs:
      upload-artifact:
        type: boolean
        default: true
      upload-tag:
        type: string
        default: "nightly"

jobs:
  test-job:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Hello
        run: echo "Test3 works"
