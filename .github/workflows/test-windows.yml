name: Test Windows

on:
  workflow_dispatch:
  push:
    branches:
      - trae/agent-arUNOy
    paths:
      - ".github/trigger-windows.txt"

jobs:
  test-windows:
    runs-on: windows-2022
    steps:
      - name: Hello
        run: echo "Windows runner works"
