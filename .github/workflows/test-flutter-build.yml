name: Test Flutter Build

on:
  workflow_dispatch:
  push:
    branches:
      - trae/agent-arUNOy
    paths:
      - ".github/trigger-flutter-build.txt"

jobs:
  test-flutter-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: false
      upload-tag: "test"
